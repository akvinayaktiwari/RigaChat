{
  "name": "vyostra-voice-relay-host",
  "version": "1.0.0",
  "private": true,
  "description": "Runtime dependencies the voice relay EC2 box must have installed. See README.md in this directory -- this file is a manifest for the host, not a buildable package.",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.658.0",
    "@aws-sdk/lib-dynamodb": "^3.658.0",
    "@aws-sdk/client-kms": "^3.1084.0"
  },
  "optionalDependencies": {
    "bufferutil": "^4.0.1",
    "utf-8-validate": ">=5.0.2"
  }
}

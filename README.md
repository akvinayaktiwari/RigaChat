# RigaChat

A SaaS platform that lets clients embed an AI chatbot, trained on their website content and knowledge base, with captured leads flowing into a built-in CRM.

## Development

Backend:

```
cd backend && npm install && npm run dev
```

Frontend:

```
cd frontend && npm install && npm run dev
```
